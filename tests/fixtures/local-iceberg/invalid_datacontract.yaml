dataContractSpecification: 1.2.1
id: orders-unit-test
info:
  title: Orders Unit Test
  version: 1.0.0
servers:
  production:
    type: local
    path: ./fixtures/local-iceberg/data/orders
    format: iceberg
    dataProductId: orders
models:
  orders:
    fields:      
      order_timestamp:
        required: true
      order_total:
        type: string
        required: true
      non_existent_field:
        type: bigint
        required: true
